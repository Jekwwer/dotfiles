# .curlrc: Configures the behavior of the curl command-line tool.

# Show a progress bar for downloads
--progress-bar

# Follow redirects automatically
--location

# Retry on transient errors up to 3 times
--retry 3

# Set a timeout for each connection attempt (15 seconds)
--connect-timeout 15

# Set a maximum time for the entire transfer (120 seconds)
--max-time 120

# Use a specific user agent string (consider using a fixed version)
--user-agent "curl/8.0"

# (Optional) Display response headers; uncomment if needed
# --include

# Specify preferred cipher suites for secure connections
--ciphers DEFAULT:@SECLEVEL=2

# Fail on HTTP errors (like 404 or 500)
--fail

# Enable IPv4 as a fallback if IPv6 is unavailable
--ipv4

# Verify SSL certificates
--ssl

# (Optional) Output verbose information for debugging; uncomment to enable
# --verbose
