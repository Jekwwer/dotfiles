# .curlrc: Configures the behavior of the curl command-line tool.

# Enable silent mode to suppress progress output
--silent

# Show errors even in silent mode
--show-error

# Follow redirects automatically
--location

# Retry on transient errors up to 3 times
--retry 3

# Set a timeout for each connection attempt (15 seconds)
--connect-timeout 15

# Set a maximum time for the entire transfer (180 seconds)
--max-time 120

# Prevent saving any output unless explicitly redirected
--no-buffer

# Use a specific user agent string
--user-agent "curl/$(curl --version | awk 'NR==1 {print $2}')"

# Display response headers
--include

# Specify preferred cipher suites for secure connections
--ciphers DEFAULT:@SECLEVEL=2

# Fail silently on HTTP errors (like 404 or 500)
--fail

# Enable IPv4 as a fallback if IPv6 is unavailable
--ipv4

# Verify SSL certificates
--ssl

# Specify CA certificate bundle (uncomment and edit if needed)
# --cacert /path/to/cacert.pem

# Output verbose information for debugging (uncomment to enable)
# --verbose

# Output to a default file (avoids overwriting with redirections)
--output /tmp/curl-output.txt
